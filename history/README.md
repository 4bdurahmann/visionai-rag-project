# history/

Q&A transcripts and run logs from the API and CLI tools. Keep a dated copy of
any prompt → response you want to track, e.g. `history/2026-08-19-eval.md`.

Not committed by default (`.gitignore` excludes `history/*.txt`); uncomment in
`.gitignore` if you want to track them.